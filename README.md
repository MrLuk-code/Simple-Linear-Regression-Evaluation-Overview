# Simple-Linear-Regression-Evaluation-Overview
This is a classic marketing mix modeling problem. To set you up for a stellar project submission, let’s walk through the exact steps to build

# Simple Linear Regression - Marketing ROI Analysis

## Project Overview
This project uses Python and `statsmodels` to build a Simple Linear Regression model that analyzes the impact of various marketing channels (TV, Radio, Social Media) on Sales. 

### `regression_analysis.ipynb`
* Ensure all cells are fully run and outputs (plots, summaries) are saved and visible directly on GitHub.
* Use Markdown cells between code blocks to explicitly detail your thoughts (e.g., *"We chose TV because its correlation coefficient of X was higher than Radio and Social Media"*).

## Environment Setup
To run the Jupyter Notebook, install the following dependencies:
```bash
pip install pandas numpy matplotlib seaborn statsmodels scipy

